name: hotel_backend
description: A hotel booking backend system built with Dart Shelf.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  mysql1: ^0.20.0
  crypto: ^3.0.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.0
